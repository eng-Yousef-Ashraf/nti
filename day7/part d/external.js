const welcome=(name)=>`Welcome, ${name}!` //16
/////////////////////////////////////////////////////
const fullInfo=(first, last, age)=>`${first} ${last} is ${age} years old`  //17
/////////////////////////////////////////////////////////////////////////////////
const multiply=(a,b)=>a*b
const sum=(a,b)=>{
    console.log(a);
    console.log(b)
    return a+b
}
//18